version: "3"
services:
  base:
    build: docker/base/
    image: gcs-base:0.1
  webservice:
    cap_add:
      - NET_ADMIN
    build: .
    volumes:
      - "./opt:/opt/gcs"
    image: gcs-main:0.1
    network_mode: "host" 
    depends_on:
      - base
    